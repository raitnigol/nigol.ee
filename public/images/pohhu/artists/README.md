# Certified artist photos

| File | Artist |
|------|--------|
| `kevilnius.jpg` | Kevilnius profile photo (used instead of Spotify avatar when present) |

You can also use `.png` or `.webp` — update `profileImage` in `src/data/pohhu.ts` if needed.
